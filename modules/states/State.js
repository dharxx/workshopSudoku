const State = {
    //enum
    default:"default",
    wrong:"wrong",
    done:"done",
    mergeStates: (states)=>{
        return 0
    },
    createState: (noDuplicate,noEmpty)=>{
        return 0
    }
}